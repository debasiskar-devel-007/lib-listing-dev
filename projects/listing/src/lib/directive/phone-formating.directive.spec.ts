import { PhoneFormatingDirective } from './phone-formating.directive';

describe('PhoneFormatingDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneFormatingDirective();
    expect(directive).toBeTruthy();
  });
});
