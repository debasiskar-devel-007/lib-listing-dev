import { LanguageTransletPipe } from './language-translet.pipe';

describe('LanguageTransletPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageTransletPipe();
    expect(pipe).toBeTruthy();
  });
});
